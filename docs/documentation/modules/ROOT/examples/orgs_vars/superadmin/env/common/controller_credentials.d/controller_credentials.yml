---
controller_credentials:
  - name: "{{ orgs }}-{{ env }}-COMMON-MACHINE-CREDENTIAL"
    description: "{{ orgs }} {{ env }} Common Machine Credential: used on pre and pro environments"
    credential_type: "Machine"
    organization: "{{ orgs }}"
    inputs:
      username: "<username>"
      password: "<password>"
...
