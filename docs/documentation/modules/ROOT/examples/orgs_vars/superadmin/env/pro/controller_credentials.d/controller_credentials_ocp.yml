---
controller_credentials:
  - name: "{{ orgs }} {{ env }} OCP Credential"
    description: "OCP SA to run Container Groups"
    credential_type: "OpenShift or Kubernetes API Bearer Token"
    organization: "{{ orgs }}"
    inputs:
      host: "https://api.cluster.pro.lab.example.com:6443"
      verify_ssl: true
      bearer_token: "...bearer_token_ocp_api_cluster_pro_lab..."
...
