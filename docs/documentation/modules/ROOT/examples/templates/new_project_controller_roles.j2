---
## available roles: admin_role, execute_role, project_admin_role, inventory_admin_role, credential_admin_role, workflow_admin_role, notification_admin_role, job_template_admin_role, execution_environment_admin_role, auditor_role, member_role, read_role, approval_role
controller_roles:
  # USER ROLES
  # {{ admin_user }}
  - user: "{{ admin_user }}"
    organizations:
      - "{{ organization_name }}"
    role: admin
  - user: "{{ admin_user }}"
    job_templates:
      - "{{ organization_name }} CasC Ctrl Config"
    role: read
  - user: "{{ admin_user }}"
    job_templates:
      - "{{ organization_name }} CasC Ctrl Drop Diff"
    role: read
  - user: "{{ organization_name | lower | regex_replace(' ','_') }}_casc_user"
    organizations:
      - "{{ organization_name }}"
    role: admin
...
